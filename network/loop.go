package network

// HandlerLoop is kept for API compatibility but is no longer used.
// Protocol sessions are now driven by lss.Run(ctx, round, sessionNetwork).
// See node/session.go for the updated session execution pattern.
